import type React from "react"
export const metadata = {
  title: "Shipping ,Return and Delivery Policy",
  description: "Shipping ,Return and Delivery Policy for using the CourierWallah website",
}

export default function TermsLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}